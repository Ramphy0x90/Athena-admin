import { TextToTitlePipe } from './text-to-title.pipe';

describe('TextToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TextToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
